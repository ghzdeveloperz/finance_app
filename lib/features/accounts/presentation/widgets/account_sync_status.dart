import 'package:flutter/material.dart';

class AccountSyncStatus extends StatelessWidget {
  const AccountSyncStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Sync OK');
  }
}